import { notFound } from "next/navigation"; import KnowledgePageTemplate from "@/components/seo/KnowledgePageTemplate"; import FAQSchema from "@/components/seo/FAQSchema"; import BreadcrumbSchema from "@/components/seo/BreadcrumbSchema"; import { countries, getCountryBySlug } from "@/data/seo/countries"; import { buildMetadata } from "@/lib/seo/metadata"; import { countrySeoLocales } from "@/lib/seo/alternates"; import InternalLinks from "@/components/seo/InternalLinks"; import { getInternalLinkSections } from "@/lib/seo/internalLinks"; import { buildSwedishCountryLocalization, swedishCountryName } from "@/lib/localization/knowledge-sv"; import { buildDanishCountryLocalization, danishCountryName } from "@/lib/localization/knowledge-da"; import { buildPolishCountryLocalization, polishCountryName } from "@/lib/localization/knowledge-pl"; import { applyKnowledgeLocalization } from "@/lib/localization/entity"; import { getLocaleDefinition } from "@/lib/localization/locales";
type PageProps = { params: Promise<{ locale: string; slug: string }> }; export function generateStaticParams() { return countries.flatMap((country) => countrySeoLocales.map((locale) => ({ locale, slug: country.slug }))); }
function localizeCountry(country: NonNullable<ReturnType<typeof getCountryBySlug>>, locale: string) { if (locale === "sv") return applyKnowledgeLocalization(country, buildSwedishCountryLocalization(country)); if (locale === "da") return applyKnowledgeLocalization(country, buildDanishCountryLocalization(country)); if (locale === "pl") return applyKnowledgeLocalization(country, buildPolishCountryLocalization(country)); return country; }
export async function generateMetadata({ params }: PageProps) { const { locale, slug } = await params; if (!countrySeoLocales.includes(locale as (typeof countrySeoLocales)[number])) return {}; const country = getCountryBySlug(slug); if (!country) return {}; return buildMetadata({ entity: localizeCountry(country, locale), locale, pathPrefix: "countries", availableLocales: countrySeoLocales }); }
export default async function CountryPage({ params }: PageProps) { const { locale, slug } = await params; if (!countrySeoLocales.includes(locale as (typeof countrySeoLocales)[number])) notFound(); const country = getCountryBySlug(slug); if (!country) notFound(); const entity = localizeCountry(country, locale); const labels = getLocaleDefinition(locale as (typeof countrySeoLocales)[number]).labels; const name = locale === "sv" ? swedishCountryName(country.name) : locale === "da" ? danishCountryName(country.name) : locale === "pl" ? polishCountryName(country.name) : country.name; const internalLinkSections = getInternalLinkSections("country", country.slug, locale); const facts = locale === "sv" ? [{ label: "Land", value: name }, { label: "Regelverk", value: "EU261 när tillämpligt" }, { label: "Högsta standardersättning", value: "€600" }] : locale === "da" ? [{ label: "Land", value: name }, { label: "Regelsæt", value: "EU261 når relevant" }, { label: "Højeste standardkompensation", value: "€600" }] : locale === "pl" ? [{ label: "Kraj", value: name }, { label: "Przepisy", value: "EU261, gdy ma zastosowanie" }, { label: "Maksymalne standardowe odszkodowanie", value: "€600" }] : [{ label: "Country", value: country.name }, { label: "Regulation", value: "EU261" }, { label: "Maximum compensation", value: "€600" }]; return <main className="min-h-screen bg-slate-50"><FAQSchema items={entity.faq} /><BreadcrumbSchema items={[{ name: labels.home, url: `https://www.flightclaimly.com/${locale}` }, { name: locale === "sv" ? "Länder" : locale === "da" ? "Lande" : locale === "pl" ? "Kraje" : "Countries", url: `https://www.flightclaimly.com/${locale}/countries` }, { name, url: `https://www.flightclaimly.com/${locale}/countries/${country.slug}` }]} /><KnowledgePageTemplate entity={entity} checkUrl={`/${locale}/check/direct-or-layover`} locale={locale} facts={facts} labels={labels} /><InternalLinks sections={internalLinkSections} /></main>; }
