import { notFound } from "next/navigation";
import { getRouteBySlug, routes } from "@/data/seo/routes"; import KnowledgePageTemplate from "@/components/seo/KnowledgePageTemplate"; import RelatedRoutes from "@/components/seo/RelatedRoutes"; import InternalLinks from "@/components/seo/InternalLinks"; import { getInternalLinkSections } from "@/lib/seo/internalLinks"; import { getRelatedRoutes } from "@/lib/knowledge/routes"; import BreadcrumbSchema from "@/components/seo/BreadcrumbSchema"; import FAQSchema from "@/components/seo/FAQSchema"; import { resolveAuthority } from "@/lib/authority/resolver"; import type { FlightRoute } from "@/data/seo/routes"; import { routeSeoLocales } from "@/lib/seo/alternates"; import { buildMetadata } from "@/lib/seo/metadata";
import { buildSwedishRouteLocalization, swedishCityName, swedishCountryName } from "@/lib/localization/knowledge-sv"; import { buildDanishRouteLocalization, danishCityName, danishCountryName } from "@/lib/localization/knowledge-da"; import { buildPolishRouteLocalization, polishCityName, polishCountryName } from "@/lib/localization/knowledge-pl"; import { buildGermanRouteLocalization, germanCityName, germanCountryName } from "@/lib/localization/knowledge-de"; import { buildFinnishRouteLocalization, finnishCityName, finnishCountryName } from "@/lib/localization/knowledge-fi"; import { buildDutchRouteLocalization, dutchCityName, dutchCountryName } from "@/lib/localization/knowledge-nl"; import { applyKnowledgeLocalization } from "@/lib/localization/entity"; import { getLocaleDefinition } from "@/lib/localization/locales";
const PREVIEW_STATIC_SAMPLE_SIZE=24; export const dynamicParams=true; type Props={params:Promise<{locale:string;slug:string}>};
function getRoutesForStaticBuild(){return process.env.VERCEL_ENV==="preview"?routes.slice(0,PREVIEW_STATIC_SAMPLE_SIZE):routes;} export function generateStaticParams(){return getRoutesForStaticBuild().flatMap(route=>routeSeoLocales.map(locale=>({locale,slug:route.slug})));}
function localizeRoute(route:FlightRoute,locale:string){if(locale==="sv")return applyKnowledgeLocalization(route,buildSwedishRouteLocalization(route));if(locale==="da")return applyKnowledgeLocalization(route,buildDanishRouteLocalization(route));if(locale==="pl")return applyKnowledgeLocalization(route,buildPolishRouteLocalization(route));if(locale==="de")return applyKnowledgeLocalization(route,buildGermanRouteLocalization(route));if(locale==="fi")return applyKnowledgeLocalization(route,buildFinnishRouteLocalization(route));if(locale==="nl")return applyKnowledgeLocalization(route,buildDutchRouteLocalization(route));return route;}
function cityName(v:string,l:string){if(l==="sv")return swedishCityName(v);if(l==="da")return danishCityName(v);if(l==="pl")return polishCityName(v);if(l==="de")return germanCityName(v);if(l==="fi")return finnishCityName(v);if(l==="nl")return dutchCityName(v);return v;} function countryName(v:string,l:string){if(l==="sv")return swedishCountryName(v);if(l==="da")return danishCountryName(v);if(l==="pl")return polishCountryName(v);if(l==="de")return germanCountryName(v);if(l==="fi")return finnishCountryName(v);if(l==="nl")return dutchCountryName(v);return v;}
export async function generateMetadata({params}:Props){const{locale,slug}=await params;if(!routeSeoLocales.includes(locale as (typeof routeSeoLocales)[number]))return{};const route=getRouteBySlug(slug);if(!route)return{};return buildMetadata({entity:localizeRoute(route,locale),locale,pathPrefix:"routes",availableLocales:routeSeoLocales});}
export default async function RoutePage({params}:Props){const{locale,slug}=await params;if(!routeSeoLocales.includes(locale as (typeof routeSeoLocales)[number]))notFound();const route=getRouteBySlug(slug);if(!route)notFound();const entity=localizeRoute(route,locale),labels=getLocaleDefinition(locale as (typeof routeSeoLocales)[number]).labels,oc=cityName(route.origin.city,locale),dc=cityName(route.destination.city,locale),oco=countryName(route.origin.country,locale),dco=countryName(route.destination.country,locale);const authoritySources=resolveAuthority<FlightRoute>({entityType:"route",slug:route.slug,entity:route});const facts=locale==="nl"?[{label:"Route",value:`${oc} → ${dc}`},{label:"Vertrekluchthaven",value:`${route.origin.name} (${route.origin.iata})`},{label:"Bestemmingsluchthaven",value:`${route.destination.name} (${route.destination.iata})`},{label:"Landen",value:`${oco} → ${dco}`}]:locale==="fi"?[{label:"Lentoreitti",value:`${oc} → ${dc}`},{label:"Lähtölentoasema",value:`${route.origin.name} (${route.origin.iata})`},{label:"Määränpään lentoasema",value:`${route.destination.name} (${route.destination.iata})`},{label:"Maat",value:`${oco} → ${dco}`}]:[{label:"Route",value:`${oc} → ${dc}`},{label:"Origin airport",value:`${route.origin.name} (${route.origin.iata})`},{label:"Destination airport",value:`${route.destination.name} (${route.destination.iata})`},{label:"Countries",value:`${oco} → ${dco}`}];const relatedRoutes=getRelatedRoutes(route),internalLinkSections=getInternalLinkSections("route",route.slug,locale);const hub=locale==="sv"?"Flygsträckor":locale==="da"?"Flyruter":locale==="pl"?"Trasy lotów":locale==="de"?"Flugstrecken":locale==="fi"?"Lentoreitit":locale==="nl"?"Vliegroutes":"Routes";return <><BreadcrumbSchema items={[{name:labels.home,url:`https://www.flightclaimly.com/${locale}`},{name:hub,url:`https://www.flightclaimly.com/${locale}/routes`},{name:`${oc}–${dc}`,url:`https://www.flightclaimly.com/${locale}/routes/${route.slug}`}]} /><FAQSchema items={entity.faq}/><KnowledgePageTemplate entity={entity} checkUrl={`/${locale}/check`} facts={facts} locale={locale} authoritySources={authoritySources} labels={labels}/><InternalLinks sections={internalLinkSections}/><RelatedRoutes title={locale==="nl"?`Meer routes vanuit ${oc}`:locale==="fi"?`Lisää lentoreittejä kaupungista ${oc}`:`More routes from ${oc}`} routes={relatedRoutes} locale={locale}/></>;}
