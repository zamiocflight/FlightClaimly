// src/app/rights/page.tsx
import type { Metadata } from "next";
import { getTranslations } from "next-intl/server";
import { useTranslations } from "next-intl";

export async function generateMetadata(): Promise<Metadata> {
  const t = await getTranslations("rights.meta");
  return {
    title: t("title"),
    description: t("description"),
  };
}

export default function RightsPage() {
  const t = useTranslations("rights");

  return (
    <main className="min-h-screen bg-white">
      <div className="max-w-3xl mx-auto px-6 py-12 text-slate-900">
        <header className="mb-8">
          <p className="text-xs font-semibold uppercase tracking-[0.2em] text-emerald-600">
            {t("badge")}
          </p>
          <h1 className="mt-2 text-3xl md:text-4xl font-bold tracking-tight text-slate-900">
            {t("title")}
          </h1>
          <p className="mt-3 text-sm md:text-base text-slate-600">
            {t("intro")}
          </p>
        </header>

        <section className="space-y-8 text-sm md:text-base text-slate-700">
          {/* När gäller EU261? */}
          <div>
            <h2 className="text-lg font-semibold text-slate-900">
              {t("whenApplies.title")}
            </h2>
            <p className="mt-2">{t("whenApplies.intro")}</p>
            <ul className="mt-2 ml-5 list-disc space-y-1">
              {(t.raw("whenApplies.bullets") as string[]).map((item, idx) => (
                <li key={idx}>{item}</li>
              ))}
            </ul>
            <p className="mt-2 text-sm text-slate-600">
              {t("whenApplies.note")}
            </p>
          </div>

          {/* Vilka situationer omfattas? */}
          <div>
            <h2 className="text-lg font-semibold text-slate-900">
              {t("covered.title")}
            </h2>
            <p className="mt-2">{t("covered.lead")}</p>
            <ul className="mt-2 ml-5 list-disc space-y-1">
              {(t.raw("covered.bullets") as string[]).map((item, idx) => (
                <li key={idx}>{item}</li>
              ))}
            </ul>
            <p className="mt-2">{t("covered.after")}</p>
          </div>

          {/* Ersättningsnivåer */}
          <div>
            <h2 className="text-lg font-semibold text-slate-900">
              {t("amounts.title")}
            </h2>
            <p className="mt-2">{t("amounts.intro")}</p>
            <ul className="mt-2 ml-5 list-disc space-y-1">
              {(t.raw("amounts.bullets") as string[]).map((item, idx) => (
                <li key={idx}>{item}</li>
              ))}
            </ul>
            <p className="mt-2 text-sm text-slate-600">
              {t("amounts.note")}
            </p>
          </div>

          {/* Undantag / extraordinära omständigheter */}
          <div>
            <h2 className="text-lg font-semibold text-slate-900">
              {t("exceptions.title")}
            </h2>
            <p className="mt-2">{t("exceptions.intro")}</p>
            <ul className="mt-2 ml-5 list-disc space-y-1">
              {(t.raw("exceptions.bullets") as string[]).map((item, idx) => (
                <li key={idx}>{item}</li>
              ))}
            </ul>
            <p className="mt-2">
              {t("exceptions.afterBeforeStrong")}
              <span className="font-medium">{t("exceptions.afterStrong")}</span>
              {t("exceptions.afterTail")}
            </p>
          </div>

          {/* Vad behöver du ha kvar? */}
          <div>
            <h2 className="text-lg font-semibold text-slate-900">
              {t("save.title")}
            </h2>
            <p className="mt-2">{t("save.intro")}</p>
            <ul className="mt-2 ml-5 list-disc space-y-1">
              {(t.raw("save.bullets") as string[]).map((item, idx) => (
                <li key={idx}>{item}</li>
              ))}
            </ul>
            <p className="mt-2 text-sm text-slate-600">
              {t("save.note")}
            </p>
          </div>

          {/* Länkar vidare */}
          <div>
            <h2 className="text-lg font-semibold text-slate-900">
              {t("links.title")}
            </h2>
            <p className="mt-2">{t("links.lead")}</p>

            <ul className="mt-2 ml-5 list-disc space-y-1">
              <li>
                <a
                  href="/delays"
                  className="
                    font-medium text-slate-900 underline-offset-2
                    hover:underline hover:text-slate-700
                    transition-colors duration-150
                  "
                >
                  {t("links.delaysText")}
                </a>{" "}
                {t("links.delaysAfter")}
              </li>
              <li>
                <a
                  href="/cancellations"
                  className="
                    font-medium text-slate-900 underline-offset-2
                    hover:underline hover:text-slate-700
                    transition-colors duration-150
                  "
                >
                  {t("links.cancellationsText")}
                </a>{" "}
                {t("links.cancellationsAfter")}
              </li>
              <li>
                <a
                  href="/faq"
                  className="
                    font-medium text-slate-900 underline-offset-2
                    hover:underline hover:text-slate-700
                    transition-colors duration-150
                  "
                >
                  {t("links.faqText")}
                </a>{" "}
                {t("links.faqAfter")}
              </li>
            </ul>
          </div>
        </section>

        <footer className="mt-10 border-t border-slate-200 pt-6 text-sm text-slate-600">
          <div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between">
            <p>{t("footer.lead")}</p>
            <a
              href="/#precheck"
              className="
                inline-flex items-center justify-center
                rounded-full border border-slate-900
                bg-slate-900 px-5 py-2 text-xs font-semibold text-white
                shadow-sm
                whitespace-nowrap
                transition-all duration-200 ease-out
                hover:bg-slate-800 hover:shadow-md
                active:scale-[0.98]
                focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-slate-900 focus-visible:ring-offset-2
              "
            >
              {t("footer.cta")}
            </a>
          </div>
        </footer>
      </div>
    </main>
  );
}
