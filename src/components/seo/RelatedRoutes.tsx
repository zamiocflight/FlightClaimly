import Link from "next/link";import type { FlightRoute } from "@/data/seo/routes";import { swedishCityName } from "@/lib/localization/knowledge-sv";import { danishCityName } from "@/lib/localization/knowledge-da";import { polishCityName } from "@/lib/localization/knowledge-pl";import { germanCityName } from "@/lib/localization/knowledge-de";import { finnishCityName } from "@/lib/localization/knowledge-fi";import { dutchCityName } from "@/lib/localization/knowledge-nl";type RelatedRoutesProps={title:string;routes:FlightRoute[];locale:string};function cityName(v:string,l:string){if(l==="sv")return swedishCityName(v);if(l==="da")return danishCityName(v);if(l==="pl")return polishCityName(v);if(l==="de")return germanCityName(v);if(l==="fi")return finnishCityName(v);if(l==="nl")return dutchCityName(v);return v;}export default function RelatedRoutes({title,routes,locale}:RelatedRoutesProps){if(routes.length===0)return null;return <section className="px-6 py-16"><div className="mx-auto max-w-5xl"><h2 className="text-3xl font-bold tracking-tight text-slate-950">{title}</h2><div className="mt-8 grid gap-4 md:grid-cols-2">{routes.map(route=><Link key={route.slug} href={`/${locale}/routes/${route.slug}`} className="rounded-2xl border border-slate-200 p-5 transition hover:border-emerald-400 hover:shadow-sm"><h3 className="text-lg font-semibold text-slate-950">{cityName(route.origin.city,locale)} → {cityName(route.destination.city,locale)}</h3><p className="mt-2 text-sm text-slate-600">{route.origin.name} ({route.origin.iata}) {locale==="sv"?"till":locale==="da"?"til":locale==="pl"?"do":locale==="de"?"nach":locale==="fi"?"–":locale==="nl"?"naar":"to"} {route.destination.name} ({route.destination.iata})</p></Link>)}</div></div></section>;}
